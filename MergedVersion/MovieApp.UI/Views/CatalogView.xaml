<UserControl
    x:Class="MovieApp.UI.Views.CatalogView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:components="using:MovieApp.UI.Views.Components"
    xmlns:models="using:MovieApp.Core.Models"
    xmlns:viewmodels="using:MovieApp.UI.ViewModels">

    <Grid Padding="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0" Text="Movie Catalog"
                   FontSize="28" FontWeight="Bold" Margin="0,0,0,16"/>

        <!-- Search and Filters -->
        <Grid Grid.Row="1" Margin="0,0,0,16">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="200"/>
                <ColumnDefinition Width="200"/>
            </Grid.ColumnDefinitions>

            <!-- Search Box -->
            <TextBox Grid.Column="0"
                     x:Name="SearchBox"
                     PlaceholderText="Search movies by title..."
                     Text="{x:Bind ViewModel.SearchQuery, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     Margin="0,0,12,0"/>

            <!-- Genre Filter -->
            <ComboBox Grid.Column="1"
                      x:Name="GenreFilter"
                      PlaceholderText="Filter by Genre"
                      ItemsSource="{x:Bind ViewModel.Genres}"
                      SelectedItem="{x:Bind ViewModel.SelectedGenre, Mode=TwoWay}"
                      Margin="0,0,12,0"
                      HorizontalAlignment="Stretch"/>

            <!-- Rating Slider -->
            <StackPanel Grid.Column="2" Orientation="Vertical">
                <TextBlock Text="{x:Bind ViewModel.MinimumRating, Mode=OneWay}"
                           FontSize="12" HorizontalAlignment="Center"/>
                <components:StarSlider Value="{x:Bind ViewModel.MinimumRating, Mode=TwoWay}"
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

        <!-- Movie Grid -->
        <GridView Grid.Row="2"
                  x:Name="MovieList" 
                  ItemsSource="{x:Bind ViewModel.Movies, Mode=OneWay}"
                  SelectionMode="None"
                  IsItemClickEnabled="True"
                  ItemClick="MovieList_ItemClick"
                  Padding="0,12,0,0">
            <GridView.ItemsPanel>
                <ItemsPanelTemplate>
                    <ItemsWrapGrid Orientation="Horizontal" MaximumRowsOrColumns="3" HorizontalAlignment="Center"/>
                </ItemsPanelTemplate>
            </GridView.ItemsPanel>
            <GridView.ItemTemplate>
                <DataTemplate x:DataType="models:Movie">
                    <Grid Width="220" Margin="8" Padding="0" CornerRadius="12" 
                          Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                          BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="330"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <!-- Poster -->
                        <Border Grid.Row="0" CornerRadius="12,12,0,0">
                            <Image Source="{Binding PosterUrl, TargetNullValue='https://via.placeholder.com/220x330?text=No+Image', FallbackValue='https://via.placeholder.com/220x330?text=Error'}" 
                                   Stretch="UniformToFill" />
                        </Border>

                        <!-- Rating Badge (Overlay) -->
                        <Border Grid.Row="0" VerticalAlignment="Top" HorizontalAlignment="Right" 
                                Margin="12" Padding="8,4" CornerRadius="6"
                                Background="#CC000000">
                            <StackPanel Orientation="Horizontal">
                                <TextBlock Text="{x:Bind AverageRating}" FontSize="14" FontWeight="Bold" Foreground="White" VerticalAlignment="Center"/>
                                <TextBlock Text="⭐" FontSize="12" Margin="4,0,0,0" VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>

                        <!-- Movie Info -->
                        <StackPanel Grid.Row="1" Padding="12">
                            <TextBlock Text="{x:Bind Title}" FontSize="16" FontWeight="Bold" MaxLines="2" TextWrapping="Wrap" TextTrimming="CharacterEllipsis"/>
                            <TextBlock FontSize="12" Foreground="{ThemeResource TextFillColorSecondaryBrush}" Margin="0,4,0,0">
                                <Run Text="{x:Bind ReleaseYear}"/>
                                <Run Text=" • "/>
                                <Run Text="{x:Bind GenreDisplay}"/>
                            </TextBlock>
                        </StackPanel>
                    </Grid>
                </DataTemplate>
            </GridView.ItemTemplate>
        </GridView>
    </Grid>
</UserControl>
